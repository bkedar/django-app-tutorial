pipeline:
  identifier: Build_django_app_tutorial_1687411023974
  name: Build django-app-tutorial
  orgIdentifier: default
  projectIdentifier: PythonCI
  properties:
    ci:
      codebase:
        build: <+input>
        connectorRef: account.Github_OAuth_1687410205905
        repoName: bkedar/django-app-tutorial
  stages:
    - stage:
        identifier: build
        name: build
        spec:
          cloneCodebase: true
          execution:
            steps:
              - step:
                  identifier: dockerbuild
                  name: docker_build
                  spec:
                    image: plugins/docker
                    settings:
                      dry_run: "true"
                      repo: hello/world
                      tags: latest
                    connectorRef: account.harnessImage
                    imagePullPolicy: IfNotPresent
                  timeout: ""
                  type: Plugin
              - step:
                  type: Run
                  name: Run_BuildCommandForPython
                  identifier: Run_BuildCommandForPython
                  spec:
                    shell: Sh
                    command: |-
                      pip install Django
                      pip install virtualenv
                      python manage.py test
          platform:
            arch: Amd64
            os: Linux
          runtime:
            spec: {}
            type: Cloud
        type: CI
